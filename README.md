# LLM Cost Autopilot

An adaptive, multi-provider LLM inference router: an async policy engine that classifies query complexity, estimates cost/quality/latency per candidate model, applies user-defined budget constraints, and selects the optimal model — with independent post-generation quality verification, budget-aware escalation, live self-correcting quality scores, and cross-provider failover.

Built solo, end-to-end. Every number below was generated by the person building it, re-verified after every regression, and never taken on faith from an automated summary — a discipline this project's own history proves was necessary (see the "Real Engineering Problems" section).

---

## The Problem

Companies running LLM-powered products routinely send *every* query — a one-word factual lookup or a deep multi-step reasoning task — to the same expensive, top-tier model. That's real, avoidable spend at scale. It's also a genuine, funded product category: OpenRouter, Not Diamond, Martian, Portkey, and LiteLLM all exist to solve exactly this problem commercially.

---

## Architecture

```
User Request  ──[ X-API-Key ]──▶  Auth (Supabase-backed)
     │
     ▼
Validate Constraints
     │
     ▼
Complexity Classifier (rule-based, production default — see below)
     │
     ▼
Candidate Models (Model Registry, quality fetched live per request)
     │
     ▼
For each candidate: availability · context window · quality ≥ floor
                     · estimated cost ≤ budget (pre-call, no API call)
     │
     ▼
Routing Policy ── economy / balanced / quality / latency
     │
     ▼
Select Best Eligible Model
     │
     ▼
Generate Response  (async, full provider abstraction: Groq + Gemini)
     │
     ▼
Quality Verification ── independent LLM-judge scoring
     │
     ├── Pass ──▶ Return
     └── Fail ──▶ Check stronger candidates → Check budget → Escalate → Re-verify → Return
     │
     ▼
Full Telemetry → Supabase (requests → model_calls → quality_scores)
     │
     ▼
Feeds back into live quality scoring for future requests
```

**Resilience:** every network call — Groq and Gemini alike — has real, tested failure handling. Rate limits and quota exhaustion trigger graceful cross-provider fallback (verified with a forced-failure test: 8/8 calls degraded correctly, zero crashes) rather than an unhandled exception.

---

## Final Verified Results

Measured on a balanced, hand-labeled 30-prompt benchmark (10 simple / 10 medium / 10 complex). These are the numbers from the last runs actually executed and read by the person building this — not a claimed or summarized figure.

| Metric | Result |
|---|---|
| Overall cost savings vs. always-premium baseline | **51.1%** (80.3% simple / 19.2% medium / 100% complex) |
| Quality retention (LLM-as-judge vs. reference model) | **4.22 / 5** across 18 judged queries |
| Classifier accuracy (rule-based, production default) | **73.3%** vs. human-labeled ground truth |
| Classifier accuracy (LLM-based, benchmarked alternative) | 66.7% — less reliable, not used in production (see below) |
| Cross-provider fallback | Verified 8/8 under forced Gemini quota exhaustion — zero unhandled crashes |

**One honest caveat:** the complex-tier quality numbers above were measured while Gemini's free-tier daily quota was already exhausted from same-day testing — every complex-tier request in that run actually resolved via fallback to `openai/gpt-oss-120b`, not Gemini directly. The fallback worked exactly as designed; the number reflects fallback-path quality, not Gemini's quality in isolation.

---

## Why the Rule-Based Classifier Is the Production Default

This is the project's most instructive finding, and it happened twice.

The LLM-based classifier originally scored 100% accuracy. After Groq deprecated the model it depended on (`llama-3.1-8b-instant`, June 2026), accuracy silently collapsed to 56–67% — root-caused via raw response inspection to reasoning-token consumption: the replacement model (`gpt-oss-120b`) spends part of its output budget on invisible internal reasoning before answering, and how much varies unpredictably per prompt, sometimes consuming the entire budget and returning nothing.

The same reasoning-token behavior resurfaced a second time in the **generation path** (not just classification) after the initial fix — `simple`-tier answers started coming back empty or truncated for the same underlying reason, requiring another round of `MAX_TOKENS_BY_TIER` tuning to give the model room to think and still answer.

**Decision, both times:** measure, don't assume. The rule-based classifier (deterministic, free, stable at 73.3%) remains the production default. The LLM-based and embedding-based (KNN) classifiers remain in the codebase, benchmarked and documented, as evidence-based alternatives rather than defaults chosen for novelty.

---

## Real Engineering Problems Found & Fixed

Documented in full because the debugging process is the actual portfolio, not just the final numbers.

1. **Token-count skew inverted expected savings** — uncapped `max_tokens` let a cheaper model generate far more output tokens than a pricier one for the same prompt. Fixed with tier-specific caps.
2. **Evaluation-integrity corruption from rate-limit fallback** — automatic fallback initially applied even to fixed reference/baseline models in evaluation scripts, silently comparing an answer to itself. Fixed with an `allow_fallback` flag.
3. **Quality-floor exact-equality bypass** — a model's quality tied a tier's floor instead of clearing it, due to strict `<` filtering. Fixed with honestly-scoped values and a safety margin.
4. **Balanced-mode scoring was silently just quality-mode** — raw cost values were orders of magnitude smaller than quality values, so the weighting never mattered. Fixed by normalizing all factors to a 0–1 range before weighting.
5. **Groq model deprecation** (`llama-3.1-8b-instant`, `llama-3.3-70b-versatile`, June 2026) — full registry, routing, and fallback migration to `gpt-oss-20b` / `gpt-oss-120b`.
6. **Reasoning-token consumption breaking both classification and generation** — diagnosed via raw response inspection (`repr()` on empty/truncated outputs); fixed via `reasoning_effort`/`thinking_budget` controls and increased `max_tokens` headroom, applied twice after the same failure mode resurfaced in a different code path.
7. **Cross-provider exception handling gap** — `call_model()` originally only caught Groq-specific exceptions; a real Gemini 429 crashed the request. Fixed by catching Gemini's actual exception class and adding it to the fallback chain — verified with a forced-failure test, not assumed from the fix alone.
8. **A pricing regression introduced by an unverified "audit" fix** — an automated summary claimed to "correct" Gemini's registry pricing, actually changing it from the real $0.00 free tier to a fabricated $0.075/$0.30. Caught via `git diff`, not by trusting the summary, and reverted before it could corrupt every downstream cost figure.
9. **Windows console Unicode crash risk** — emoji in `print()` warning messages could raise `UnicodeEncodeError` on `cp1252` terminals, crashing the very fallback path meant to handle failures gracefully. Replaced with plain ASCII.
10. **Full async I/O conversion** — the FastAPI endpoint was declared `async def` but blocked the event loop on every LLM/database call underneath. Converted the entire chain (providers → model dispatch → classifier/confidence → routing policy → endpoint) to genuine async I/O, layer by layer, each tested in isolation before integration.

---

## Features

- Policy-based routing across 4 modes, fully explainable (chosen model + every rejected candidate + reason)
- Multi-provider (Groq + Gemini) behind a common async interface, with verified cross-provider failover
- Pre-call cost estimation, no wasted API calls to check affordability
- Independent post-generation quality verification + budget-aware escalation
- Relational telemetry (Supabase/Postgres) with dynamic, live-computed quality scores
- API-key authentication, Docker, CI (GitHub Actions)
- Pip-installable client SDK with typed `RoutingResult`/`CandidateModel` objects
- Two Streamlit UIs: historical dashboard + live interactive query console

---

## What I'd Build Next

- Cache live quality scores on an interval rather than querying fresh per request
- Multi-user auth (Supabase Auth/JWT) if this needed to serve independently-tracked users
- Expand the evaluation benchmark beyond 30 prompts for tighter statistical confidence
- Async conversion of the logging path (deliberately left synchronous — logging is fast relative to LLM calls, so the concurrency win is concentrated where it matters most)

---

## Running It

```bash
pip install -r requirements.txt
uvicorn main:app --reload
streamlit run dashboard.py --server.port 8501
streamlit run query_console.py --server.port 8502

# Or containerized
docker build -t llm-cost-autopilot .
docker run -p 8000:8000 --env-file .env llm-cost-autopilot

# Client SDK
pip install -e .
```

Requires your own free Groq + Gemini + Supabase accounts — see `.env.example`. API access is protected by a self-issued key; no credentials ship with the code.

```bash
python classifier_accuracy.py    # classifier comparison
python compare_baseline.py       # cost savings measurement
python quality_check.py          # quality verification (LLM-as-judge)
```